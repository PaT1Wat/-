# Supabase Configuration
# ⚠️ IMPORTANT SECURITY NOTICE:
# If you previously shared your anon key (e.g., pasted it in chat or code),
# you MUST rotate it immediately in your Supabase dashboard:
# Project Settings > API > Generate new anon key
#
# Key types:
# - SUPABASE_URL: Your Supabase project URL
# - SUPABASE_ANON_KEY: Safe for client-side use, respects Row Level Security (RLS)
# - SUPABASE_SERVICE_ROLE_KEY: Server-side only! NEVER expose to client bundles.
#   This key bypasses RLS. Keep it secret and add it to GitHub Secrets, not code.
# - SUPABASE_JWT_SECRET: Used to verify Supabase Auth JWT tokens server-side.
#   Find it in Supabase Dashboard > Project Settings > API > JWT Secret

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Database Connection String (for migrations)
# This is the direct PostgreSQL connection string for running migrations.
# Find it in: Supabase Dashboard > Project Settings > Database > Connection string > URI
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT].supabase.co:5432/postgres
# Note: Add ?sslmode=require for secure connections
DATABASE_URL=postgresql://postgres:your-password@db.your-project.supabase.co:5432/postgres?sslmode=require

# Google Sign-In (Client-Only Mode - Optional)
# If you want to enable client-only Google Sign-In without requiring Supabase database:
# 1. Create OAuth 2.0 credentials in Google Cloud Console (https://console.cloud.google.com/)
# 2. Add authorized JavaScript origins (e.g., http://localhost:3000, https://yourdomain.com)
# 3. Copy the Client ID and paste it in frontend/.env as REACT_APP_GOOGLE_CLIENT_ID
# Note: This is for demo/testing purposes. For production, verify ID tokens on the server.

# See backend/.env.example for full backend configuration
# See frontend/.env.example for frontend configuration

# API Configuration
REACT_APP_API_URL=http://localhost:3001/api

# Supabase Configuration
# Get these values from your Supabase dashboard:
# Project Settings > API
REACT_APP_SUPABASE_URL=https://your-project.supabase.co
REACT_APP_SUPABASE_ANON_KEY=your-anon-key-here

# Google Sign-In (Client-Only Mode)
# Optional: If you want to use client-only Google Sign-In without Supabase/database:
# 1. Create OAuth 2.0 credentials in Google Cloud Console
# 2. Set authorized JavaScript origins (e.g., http://localhost:3000)
# 3. Add your Client ID here
# When REACT_APP_GOOGLE_CLIENT_ID is set, the app will use client-only authentication
# without requiring Supabase or a backend database.
REACT_APP_GOOGLE_CLIENT_ID=your-google-client-id-here
